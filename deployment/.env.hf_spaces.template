# Hugging Face Spaces backend environment variables.
# Use this as a copy/paste template for Spaces Variables and Secrets.
# Do not commit a real deployment/.env.huggingface file with secrets.

PORT=7860

# Spaces should use the ONNX runtime and remote model download so the container
# starts from a small image and pulls artifacts at boot.
MODEL_RUNTIME=onnx
TF_CPP_MIN_LOG_LEVEL=3
TF_ENABLE_ONEDNN_OPTS=0

KAFKA_ENABLED=false
MLFLOW_ENABLED=false
MLFLOW_STRICT=false

# Model artifacts are downloaded from the public/private model repository.
# Set HF_TOKEN as a Space Secret only when the model repo is private.
HF_MODEL_DOWNLOAD_ENABLED=true
HF_MODEL_REPO_ID=a1mohamadd/lung-disease-detection
HF_MODEL_REPO_TYPE=model
HF_MODEL_REVISION=main
HF_MODEL_REPO_SUBDIR=
# Leave empty if the Hugging Face model repo is public.
HF_TOKEN=

DB_LOGGING_ENABLED=true
DB_BACKEND=postgres
# A managed Postgres URL keeps prediction logs outside the ephemeral Space
# filesystem. Use a private/internal URL when the provider supports it.
# Render Postgres URL can still be used when the backend runs on Hugging Face.
# If the connection fails, add ?sslmode=require to the URL.
DATABASE_URL=PASTE_RENDER_POSTGRES_INTERNAL_DATABASE_URL_HERE

PREDICTION_STORAGE_BACKEND=supabase
# Supabase stores generated source/mask/ROI/overlay artifacts because Hugging
# Face Spaces storage is not a durable artifact store.
SUPABASE_URL=PASTE_SUPABASE_PROJECT_URL_HERE
SUPABASE_SERVICE_ROLE_KEY=PASTE_SUPABASE_SERVICE_ROLE_KEY_HERE
SUPABASE_STORAGE_BUCKET=lung-detection-predictions
SUPABASE_SIGNED_URL_EXPIRES_SECONDS=3600

LOGS_API_KEY=PASTE_LOGS_API_KEY_HERE

# Leave empty until GitHub Pages frontend is deployed.
# Example later: CORS_ALLOW_ORIGINS=https://a1mohamad.github.io
CORS_ALLOW_ORIGINS=
